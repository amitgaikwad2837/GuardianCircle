// Placeholder — implementation in src/features/guardian/infrastructure/GuardianNotificationHandler.ts
// Separated here to satisfy App.tsx import during scaffolding.
export const GuardianNotificationHandler = {
  initialize(): void {
    // TODO: Implement in Phase 4
    // - messaging().onMessage(handleMessage)
    // - messaging().setBackgroundMessageHandler(handleBackgroundMessage)
    // - messaging().onTokenRefresh(handleTokenRefresh)
  },
};
